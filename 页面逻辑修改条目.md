# 页面逻辑修改条目

本文档记录当前大学生工作推荐网站在页面跳转、业务流程和前后端接口衔接上的主要问题，以及建议修改方向。

## 1. 拆分 AI 规划页和推荐职位页

当前 `/recommendations/` 被普通 Django view 指向 `ai_career_navigation`，实际显示的是“上传简历做 AI 规划”的页面。但项目中同时存在 `RecommendationsPage` 和 `recommendations_page.html`，语义上更像“个性化推荐职位列表”。

建议修改：

- `/ai/plan/`：AI 规划入口，负责上传简历。
- `/ai/result/`：AI 分析报告页。
- `/recommendations/`：个性化推荐职位列表。
- 首页、底部导航、个人中心中的“AI 规划”和“推荐职位”入口分别指向对应页面。

## 2. 优化 AI 简历分析流程

当前流程中，用户上传简历后会先调用 `/api/analyze-resume/` 生成 AI 报告，然后跳转到 `/resume/edit/` 让用户确认简历信息。用户确认后又会调用 `/api/confirm-resume-info/` 再生成完整报告，存在重复分析和状态混乱。

建议修改为：

1. 上传简历。
2. 后端只做文本提取和 AI 初步字段提取。
3. 跳转到简历信息确认页。
4. 用户确认或修改信息。
5. 后端基于确认后的信息生成完整 AI 报告。
6. 跳转到 AI 报告页或推荐职位页。

## 3. 让“投递”真正记录到系统

职位详情页当前“一键前往投递”只是直接打开外部招聘链接，没有先调用后端投递接口。因此用户实际点击投递后，个人中心的“已投递”记录可能不会增加。

现状：

- 后端已有 `apply_job` 方法。
- `local/urls.py` 没有注册投递 API。
- `job_page.html` 没有调用投递 API。

建议修改：

- 新增路由：`/api/apply-job/`。
- 详情页点击投递时，先 POST 到 `/api/apply-job/`。
- 后端记录 `applied` 状态和 `applied_date`。
- 记录成功后，再打开 `source_url` 外部链接。
- 如果用户未登录，先跳转登录页，并携带 `next` 参数。

## 4. 补齐前端调用但后端不存在的接口

当前模板中存在前端调用，但 `local/urls.py` 没有对应路由，容易导致 404。

需要处理：

- `dashboard.html` 调用 `/api/update-application-notes/<id>/`。
- `recommendations.html` 调用 `/api/track-recommendation-click/`。

建议修改：

- 如果功能需要保留，补齐对应 view 和 url。
- 如果暂时不做，删除前端调用或改成无副作用的本地逻辑。
- 避免页面控制台出现无意义报错，影响后续调试。

## 5. 修正底部导航的个人中心路径

`jobs/templates/base.html` 中底部导航使用了 `/account/profile/`，但实际路由是 `/accounts/profile/`。

建议修改：

- 将 `/account/profile/` 改为 `{% url 'account_profile' %}`。
- 其他硬编码的账户路径也尽量改为 Django URL name。

## 6. 明确“收藏”和“投递”的关系

当前 `JobApplication` 使用同一条记录的 `status` 表示 `saved` 或 `applied`。如果用户先收藏再投递，状态可能从 `saved` 变为 `applied`，导致该职位从收藏列表消失。

建议修改模型语义：

- `is_saved`：是否收藏。
- `applied_at`：投递时间，空值表示未投递。
- `status`：仅表示投递进度，例如 `applied`、`contacted`、`rejected`、`accepted`。

这样一个职位可以同时处于“已收藏”和“已投递”状态，符合真实用户行为。

## 7. 统一主要用户路径

建议将学生端页面逻辑整理成三条主线。

发现职位路径：

```text
首页 / 职位列表 -> 职位详情 -> 收藏或投递 -> 个人中心
```

AI 推荐路径：

```text
首页 -> AI规划 -> 上传简历 -> 确认简历信息 -> AI报告 -> 推荐职位 -> 职位详情 -> 投递
```

个人中心路径：

```text
个人中心 -> 我的简历 / AI报告 / 收藏职位 / 已投递职位 / 编辑资料
```

## 8. 建议优先级

P0：

- 修正 `/account/profile/` 错误路径。
- 注册并接入投递 API。
- 删除或补齐不存在的前端 API 调用。

P1：

- 拆分 `/ai/plan/`、`/ai/result/`、`/recommendations/`。
- 调整 AI 简历分析流程，避免重复生成报告。
- 明确收藏和投递状态关系。

P2：

- 将硬编码 URL 逐步替换为 `{% url %}` 或 `{% pageurl %}`。
- 统一首页、职位列表页、推荐页、个人中心的入口文案。
- 为收藏、投递、AI 流程补充页面级测试或接口测试。
