# 数据库迁移说明

## 问题
当前数据库缺少新添加的字段，导致页面无法正常访问。

## 解决方案

### 方法1：运行Django迁移（推荐）

在项目根目录的 `mysite` 文件夹下，运行以下命令：

```bash
# Windows PowerShell
cd mysite
python manage.py migrate

# 或者如果manage.py在项目根目录
python manage.py migrate jobs
```

### 方法2：手动执行SQL（如果迁移失败）

如果迁移遇到问题，可以手动在MySQL中执行以下SQL：

```sql
USE project;  -- 替换为你的数据库名

ALTER TABLE jobs_studentprofile 
ADD COLUMN ai_extracted_school VARCHAR(200) NULL COMMENT 'AI提取的学校',
ADD COLUMN ai_extracted_major VARCHAR(200) NULL COMMENT 'AI提取的专业',
ADD COLUMN ai_extracted_internship_summary TEXT NULL COMMENT 'AI提取的实习经历总结',
ADD COLUMN ai_extracted_hobbies TEXT NULL COMMENT 'AI提取的爱好和职业兴趣',
ADD COLUMN ai_extracted_skills JSON NULL COMMENT 'AI提取的核心技能',
ADD COLUMN ai_extraction_completed BOOLEAN DEFAULT FALSE COMMENT 'AI初步提取完成',
ADD COLUMN ai_extraction_updated_at DATETIME NULL COMMENT 'AI提取更新时间';
```

### 方法3：检查迁移状态

查看迁移状态：
```bash
python manage.py showmigrations jobs
```

如果看到 `[ ] 0007_studentprofile_ai_extracted_fields` 未标记，说明迁移未运行。

### 验证迁移成功

迁移成功后，错误应该消失，页面可以正常访问。

## 注意事项

- 迁移前建议备份数据库
- 如果使用生产环境，请在维护窗口期间执行
- 确保Django服务已停止或使用只读模式
